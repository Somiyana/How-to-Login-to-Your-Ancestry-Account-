version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"           # ← Change this from "3.13" (or remove to use default ~3.12)

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt   # This installs Sphinx==8.2.3
